using UnityEngine;

[RequireComponent(typeof(PlayerInput))]
[RequireComponent(typeof(PlayerFlipper))]
public class PlayerPack : MonoBehaviour
{
    ///Similar to Controller Pack, this just holds all the required components.
}
